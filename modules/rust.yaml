id: rust
name: Rust
description: Systems programming language.
category: languages
install:
  linux:
    - "curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y"
check: "rustc --version"
